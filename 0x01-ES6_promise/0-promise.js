/*
 * Defines a function getResponseFromAPI
 * which returns a Promise
 */

export default function getResponsefromAPI() {
  return new Promise((resolve, reject) => {
    resolve();
    reject();
  });
}
